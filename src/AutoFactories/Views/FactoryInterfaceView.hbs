#nullable enable
using System;

{{#if Type.Namespace}}
namespace {{Type.Namespace}}
{
{{/if}}
    {{AccessModifier}} interface I{{Type.Name}}Factory
    {
        {{#each Methods}}
        /// <summary>
        /// Creates a new instance of  <see cref="{{ReturnType.QualifiedName}}"/>
        /// </summary>
        global::{{ReturnType.QualifiedName}} {{Name}}({{> Parameters}});
        {{/each}}
    }
{{#if Type.Namespace}}
}
{{/if}}