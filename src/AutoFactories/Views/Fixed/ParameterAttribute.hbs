#nullable enable
using System;

namespace {{Type.Namespace}}
{
    /// <summary>
    /// Applied to a parameter of a constructor or method to imply the factory
    /// should provide this parameter rather then the user.
    /// </summary>
    [AttributeUsage(AttributeTargets.Parameter, AllowMultiple = false)] 
    {{AccessModifier}} sealed class {{Type.Name}} : Attribute
    {
        public {{Type.Name}}()
        {}
    }
}