#nullable enable
using System;

{{#if Type.Namespace}}
namespace {{Type.Namespace}}
{
{{/if}}
    {{AccessModifier}} partial class {{Type.Name}}Factory : I{{Type.Name}}Factory
    {
        {{> factory_fields }}
        {{> factory_properties }}
        {{> factory_constructors }}


        {{#each Methods}}
        /// <summary>
        /// Creates a new instance of  <see cref="{{ReturnType.QualifiedName}}"/>
        /// </summary>
        public global::{{ReturnType.QualifiedName}} {{Name}}({{> Parameters}})
        {
            {{> factory_method}}
        }{{#unless @Last}}

{{/unless}}{{/each}}
    }
{{#if Type.Namespace}}
}
{{/if}}